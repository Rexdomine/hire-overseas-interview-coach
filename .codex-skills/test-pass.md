# Test Pass Skill

Generate and run a focused test pass for the implemented MVP. Cover happy path, invalid input, missing record, update/status behavior, and list/filter behavior. Prefer pytest + FastAPI TestClient. Keep tests small. Return test plan, commands, pass/fail result, and smallest fix if needed.
